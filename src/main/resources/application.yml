weather-api:
  port: 34000
  uri: localhost

spring:
  data:
    mongodb:
      # TODO set url from mongo DB container
      uri: mongodb://localhost:8989/test

db:
  clear:
    probability:
      percentage: 10